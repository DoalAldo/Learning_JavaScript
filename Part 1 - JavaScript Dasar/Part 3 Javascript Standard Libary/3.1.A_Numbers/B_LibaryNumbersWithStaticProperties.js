const input ="1234567";
const number = Number(input);
console.log(`Value dari Number yang di convert dari value input adalah => ${number}`);

console.info(`Min Value from numbers is ${Number.MIN_VALUE}`);
console.info(`Min Value from numbers is ${Number.MAX_VALUE}`);
