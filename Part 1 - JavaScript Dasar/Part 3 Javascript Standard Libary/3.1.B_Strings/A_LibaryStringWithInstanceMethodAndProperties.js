const nama = "Darmatius Aldo Gunawan";

console.info(`Name Length is  => ${nama.length}`);
console.info(`Name to UPPERCASE is => ${nama.toUpperCase()}`);
console.info(`Name to lowercase is => ${nama.toLowerCase()}`);


console.info(`Name with split is => ${nama.split(" ")}`);


const input = "                input Nama                    ";
console.info(input.trim());